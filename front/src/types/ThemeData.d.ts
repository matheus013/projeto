export interface ThemeDataType {
  primaryColor: string;
  sidebar: {
    backgroundColor: string;
  };
  header: {
    backgroundColor: string;
  } ;

  infoCard: {
    backgroundColor: string;
  };

  panel: {
    backgroundColor: string;
  };
};